function getComputerChoice() {
    // random value for computer player
}

// ask for user input

function playRound(playerSelection, computerSelection) {
    // compare both values and determine winner
    // increment value of won rounds
}
